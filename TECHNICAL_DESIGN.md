# Backlog 待办桌面应用技术方案

## 1. 项目范围

Backlog 是仅面向 Windows 和 macOS 的本地待办桌面应用。

- 数据默认仅保存在本机，不提供账号与云同步。
- 支持通过导出文件在不同设备间迁移数据。
- 支持富文本、Markdown、附件与本地全文搜索。
- 支持开机启动、窗口置顶、托盘、贴边折叠和自动更新。
- 视觉风格采用克制、紧凑的桌面工具布局，并根据操作系统使用原生毛玻璃效果。

## 2. 技术选型

| 模块 | 选型 | 用途 |
| --- | --- | --- |
| 桌面框架 | Electron | 窗口、托盘、快捷键、开机启动和自动更新 |
| 构建工具 | electron-vite | 管理主进程、预加载脚本和渲染进程 |
| 前端框架 | Vue 3 + TypeScript | 应用界面与交互 |
| 状态管理 | Pinia | 管理界面状态、筛选条件和编辑状态 |
| UI 基础 | Radix Vue + 自定义 CSS | 提供无障碍交互基础并保持自定义视觉风格 |
| 图标 | Lucide Vue Next | 提供统一的桌面工具图标 |
| 编辑器 | Tiptap | 富文本、任务列表、链接、图片及 Markdown 转换 |
| 本地数据库 | SQLite + `node:sqlite` | 保存待办、标签、设置和附件元数据，无需外部原生模块 |
| 全文搜索 | SQLite FTS5 | 搜索标题、正文和标签 |
| 数据校验 | Zod | 校验 IPC 参数、配置及导入数据 |
| 安装打包 | electron-builder | 生成 Windows NSIS 安装包和 macOS DMG |
| 自动更新 | electron-updater | 检查、下载并安装新版本 |
| 测试 | Vitest + Playwright | 单元测试和桌面端关键流程测试 |

不采用 Tauri 的主要原因是现有技术栈为 Vue + TypeScript，且项目不重视安装包体积。Electron 在系统集成、自动更新和桌面生态方面更适合当前需求。

## 3. 应用架构

```text
Main Process
├── 窗口与贴边折叠控制
├── 托盘、全局快捷键和开机启动
├── SQLite 数据访问与迁移
├── 附件管理与导入导出
└── 自动更新

Preload
└── 向渲染进程暴露经过校验的最小 IPC API

Vue Renderer
├── 页面与交互
├── Tiptap 编辑器
├── 搜索与筛选
└── Pinia 状态
```

数据库、文件系统和操作系统 API 只允许由主进程访问。渲染进程不得直接启用 Node.js 能力。

## 4. 安全基线

- 启用 `contextIsolation`。
- 关闭 `nodeIntegration`。
- Preload 仅暴露明确列出的业务方法，不暴露通用 IPC 调用接口。
- 所有 IPC 输入使用 Zod 校验。
- 导入文件需校验格式版本、路径、文件大小和哈希。
- 禁止导入包通过相对路径或符号链接写出目标目录。
- 外部链接经确认后交由系统浏览器打开。
- 应用页面采用内容安全策略，限制脚本和资源来源。

## 5. 本地数据设计

应用数据保存在 Electron 的 `userData` 目录：

```text
userData/
├── backlog.db
├── attachments/
│   └── <content-hash>.<extension>
├── thumbnails/
└── logs/
```

### 5.1 正文格式

- Tiptap JSON 是富文本正文的权威数据。
- 同步生成纯文本字段，供 FTS5 建立全文索引。
- Markdown 作为编辑模式及导入导出格式，不与 Tiptap JSON 同时充当权威数据。
- 在不同格式之间切换时进行显式转换，并对不支持无损转换的节点给出提示。

### 5.2 标识与时间

- 待办、标签和附件使用 UUID，避免跨设备导入时发生自增 ID 冲突。
- 时间以 UTC 保存，展示时转换为系统时区。
- 数据库使用版本化迁移，不在运行时临时修改表结构。

### 5.3 附件

- 附件按内容哈希存储，减少重复文件。
- 数据库只保存附件元数据、哈希和逻辑关联。
- 删除待办后先进入可恢复状态，再由清理任务移除无引用附件。

## 6. 全文搜索

使用 SQLite FTS5 索引以下内容：

- 待办标题
- 富文本转换得到的纯文本正文
- Markdown 正文
- 标签名称

待办写入与索引更新必须位于同一数据库事务中。中文分词能力需在原型阶段验证；若默认 tokenizer 无法达到可接受效果，再引入明确支持中文的 tokenizer，不提前增加额外搜索服务。

## 7. 数据导入与导出

导出文件使用 `.backlog` 扩展名，本质为 ZIP 容器：

```text
manifest.json
database.sqlite
attachments/
```

`manifest.json` 至少包含：

- 导出格式版本
- 应用版本
- 导出时间
- 数据库文件哈希
- 附件清单、大小与哈希

导入支持两种模式：

- 覆盖：校验并备份当前数据后，用导入数据替换现有数据。
- 合并：按 UUID 合并记录，并对冲突内容要求用户确认处理策略。

导入必须先解压到临时目录并完整校验，通过后再以事务方式写入正式数据目录。失败时保留原数据。

## 8. 窗口交互

### 8.1 基本能力

- 窗口默认并始终置顶。
- 支持开机启动。
- 支持系统托盘。
- 应用常驻系统托盘并从任务栏隐藏；单击托盘图标显示或隐藏窗口。
- 托盘右键菜单提供显示/隐藏、左右停靠、设置和退出，主窗口不提供退出按钮。
- 支持全局快捷键显示或隐藏窗口，默认使用 `CommandOrControl+Shift+/`。
- 用户可在应用设置中录制并保存新的全局快捷键。
- 窗口只允许停靠当前显示器左侧或右侧，默认停靠右侧。
- 窗口始终紧贴工作区边缘，高度固定为工作区高度，只允许调整宽度。
- 通过标题栏按钮切换停靠侧，并持久化选择。
- 已记录的显示器断开时，窗口自动迁移到主显示器可见区域。

### 8.2 显示与隐藏

- 快捷键在窗口可见时隐藏窗口，在窗口隐藏时重新停靠、显示并聚焦窗口。
- 显示器工作区或缩放比例变化时，重新计算全高停靠尺寸。
- 已占用的快捷键不得覆盖现有设置，并向用户提示重新录制。

## 9. 视觉实现

- macOS 使用 Electron `vibrancy` 接入系统原生材质。
- Windows 11 优先使用 `backgroundMaterial: "mica"` 或 `"acrylic"`。
- 不支持原生材质时，降级为半透明背景与 CSS `backdrop-filter`。
- 使用窄侧边栏、紧凑工具栏、清晰信息层级和低装饰密度。
- 不使用大面积渐变、装饰性卡片堆叠或模板化宣传页结构。
- Windows 与 macOS 保持信息结构一致，但允许原生材质和窗口行为存在平台差异。

## 10. 安装与自动更新

### Windows

- 使用 electron-builder 生成 NSIS 安装包。
- 使用非一键安装模式，允许用户选择安装目录。
- 正式发布建议配置代码签名证书，降低 SmartScreen 警告。

### macOS

- 使用 electron-builder 生成 DMG。
- 正式发布与自动更新需要 Developer ID 签名和 notarization。

### 更新流程

1. 应用启动后静默检查更新。
2. 后台下载可用版本。
3. 下载完成后提示用户重启安装。
4. 用户可暂缓更新，重要安全更新可提高提示等级。
5. 更新失败不得影响本地数据和正常启动。

更新源的具体托管服务在发布方案确定时配置，不写死在业务代码中。

## 11. 工程注意事项

- 使用 Electron 内置 Node 运行时的 `node:sqlite`，避免额外原生模块的 ABI 重建。
- Windows 与 macOS 安装包应分别在对应操作系统构建和签名。
- 数据库迁移、导入导出和自动更新属于高风险路径，需要独立测试。
- 窗口吸附需覆盖多显示器、不同缩放比例和任务栏位置。
- 所有窗口坐标使用 Electron 的 DIP 坐标体系处理，避免高 DPI 下错位。

## 12. 首期验收标准

- Windows 和 macOS 均可完成安装、启动与卸载。
- 待办可创建、编辑、删除、恢复和搜索。
- 富文本、Markdown 和附件在重启应用后保持完整。
- 导出文件可在另一台设备导入，附件与内容一致。
- 窗口始终全高紧贴屏幕左侧或右侧，不能纵向缩放或自由移动。
- 默认快捷键和用户录制的快捷键均可稳定显示或隐藏窗口。
- 多显示器切换、拔出和缩放变化不会导致窗口永久移出屏幕。
- 普通渲染页面无法直接访问 Node.js、数据库或任意文件路径。
- 自动更新失败不会破坏应用数据或阻止应用再次启动。

## 13. 已确认决策

- 目标平台：Windows、macOS。
- 数据模式：纯本地存储，支持文件导入导出。
- 前端技术：Vue 3 + TypeScript。
- 桌面框架：Electron。
- 安装方式：直接下载安装包。
- 更新方式：应用内自动更新。
- 内容能力：富文本、Markdown、附件、本地全文搜索。
- 窗口停靠：仅支持左侧、右侧，默认右侧并始终置顶。
- 窗口尺寸：高度锁定为当前工作区高度，宽度范围为 `320-560px`。
- 默认快捷键：Windows 为 `Ctrl+Shift+/`，macOS 为 `Cmd+Shift+/`，支持用户自定义。
- 语言：支持跟随系统、简体中文和 English，并持久化用户选择。
- 运行方式：应用常驻系统托盘，不显示任务栏图标，退出仅通过托盘右键菜单。
- 视觉：macOS 使用 Vibrancy，Windows 使用 Acrylic/Mica，界面采用半透明高可读性玻璃基底。
